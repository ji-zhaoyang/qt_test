# 首页前端结构说明

本文档记录 `assets/web/test_map.html` 当前已经落地的前端拆分结果。

目标不是描述“计划怎么拆”，而是说明“现在已经怎么拆完了，每个文件负责什么，后续应该往哪里改”。

---

## 1. 当前文件划分

首页网页相关代码当前主要分成 6 个文件：

- `assets/web/js/map_bootstrap.js`
- `assets/web/js/target_store.js`
- `assets/web/js/target_panel.js`
- `assets/web/js/target_actions.js`
- `assets/web/js/qt_bridge.js`
- `assets/web/js/map_core.js`

对应职责如下。

### `map_bootstrap.js`

负责地图壳和地图初始化：

- 创建 `map`
- 初始化深色态势图 / 卫星图图层
- 处理 `btn-toggle-map` 地图切换
- 创建基础图标：
  - `customIcon`
  - `droneTargetIcon`
  - `fallbackDroneTargetIcon`
- 创建主设备 `myMarker`
- 处理页面初始加载和窗口 resize 时的 `map.invalidateSize()`

这一层只负责“地图本身能不能起来”，不负责目标数据、右侧面板、Qt 协议交互。

### `target_store.js`

负责目标状态和目标缓存：

- `droneTargets`
- `droneMarkers`
- `selectedTargetId`
- `targetOrder`
- 目标过期移除定时器
- 目标增删改和选中切换

这一层的定位是“内存里的目标状态容器”。

它不应该负责：

- 拼 Qt 命令
- 解析 Qt 回调
- 渲染详情 DOM

### `target_panel.js`

负责右侧面板显示和详情渲染：

- 面板显隐
- 目标列表渲染
- 目标详情渲染
- 动作提示文案
- 精准打击 / 宽频干扰按钮文案状态

这一层的定位是“页面展示层”。

它依赖 `target_store.js` 和 `target_actions.js` 提供的状态，但自己不负责发命令。

### `target_actions.js`

负责首页目标动作逻辑：

- `directionFindingState`
- `precisionStrikeState`
- `wideJamState`
- 测向弹窗显示和进度更新
- 精准打击倒计时
- 宽频干扰状态切换
- 动作按钮点击处理
- 动作可见性判断：
  - `hasDirectionalAction()`
  - `canShowWideJam()`
  - `hasPrecisionStrike()`

这一层的定位是“前端业务动作层”。

它负责决定：

- 当前目标是否支持某个动作
- 当前按钮应该显示什么
- 用户点击后前端怎么进入 pending / active 状态

但它不负责：

- 直接操作 Qt C++ 对象
- 地图初始化
- 目标列表 DOM 结构管理

### `qt_bridge.js`

负责 Qt 和网页之间的桥接入口：

- 网页向 Qt 发命令：
  - `sendDirectionFindingCommand()`
  - `sendPrecisionStrikeCommand()`
  - `sendWideJamCommand()`
  - 全屏命令
- Qt 回网页入口：
  - `updateDroneTargetFromQt()`
  - `updateMarker()`
  - `updateDashboard()`
  - `updateDroneDirectionFindingResponseFromQt()`
  - `updateDroneDirectionPowerReportFromQt()`
  - `updateDronePrecisionStrikeResponseFromQt()`
  - `updateDroneWideBandJammingResponseFromQt()`

这一层的定位是“网页侧统一桥接入口”。

重要约束：

- Qt C++ 侧目前调用的 JS 函数名，应该尽量保持稳定
- 如果要改入口名，必须同步检查 `home_web_bridge.cpp`

### `map_core.js`

现在保留的是地图业务辅助和通用格式化：

- 坐标、距离、角度、频率等格式化函数
- 目标点位 tooltip 和 marker 更新辅助
- `ensureTargetMarker()`
- `resolveTargetPosition()`
- 定位 / 缩放按钮

它当前更像：

- 地图业务工具层

而不是以前那个“把所有逻辑都塞进去的大文件”。

---

## 2. 当前脚本加载顺序

`assets/web/test_map.html` 当前脚本顺序如下：

```html
<script src="./js/map_bootstrap.js"></script>
<script src="./js/target_store.js"></script>
<script src="./js/target_panel.js"></script>
<script src="./js/target_actions.js"></script>
<script src="./js/map_core.js"></script>
<script src="./js/qt_bridge.js"></script>
<script src="./js/map_measure.js"></script>
```

这个顺序不要随便改，原因如下：

- `map_bootstrap.js` 要先创建 `map`、`myMarker`、图层和基础 icon
- `target_store.js` 要先提供目标缓存
- `target_panel.js` 依赖 `target_store.js` 和 `target_actions.js` 的状态函数
- `target_actions.js` 依赖 store 提供的目标读取能力
- `map_core.js` 依赖 `map`、marker icon、store、panel、actions
- `qt_bridge.js` 需要在前面这些函数都准备好以后，再暴露统一 Qt 入口

简单理解：

- `map_bootstrap.js` 最靠前
- `qt_bridge.js` 靠后，作为“统一入口层”

---

## 3. Qt 到网页的调用链

当前首页 Qt 和网页的主要调用链是：

`TcpManager / Service`
-> `HomeCoordinator`
-> `HomePage`
-> `HomeWebBridge`
-> `runJavaScript(...)`
-> `qt_bridge.js` 暴露的全局函数

典型入口包括：

- `updateMarker(...)`
- `updateDashboard(...)`
- `updateDroneTargetFromQt(...)`
- `updateDroneDirectionFindingResponseFromQt(...)`
- `updateDroneDirectionPowerReportFromQt(...)`
- `updateDronePrecisionStrikeResponseFromQt(...)`
- `updateDroneWideBandJammingResponseFromQt(...)`

因此，网页侧如果只是内部重构，优先保证这些全局函数名不变，Qt 侧就不需要跟着一起改。

---

## 4. 现在建议怎么改代码

后续再改首页前端时，建议按下面规则落点。

### 改地图底图、图标、首屏布局

放到：

- `map_bootstrap.js`

### 改目标缓存、目标删除、目标选中

放到：

- `target_store.js`

### 改右侧列表、详情面板、提示文案

放到：

- `target_panel.js`

### 改测向、精准打击、宽频干扰交互

放到：

- `target_actions.js`

### 改 Qt 发命令格式或 Qt 回调入口

放到：

- `qt_bridge.js`

同时检查：

- `src/views/home/home_web_bridge.cpp`

### 改通用格式化或 marker 展示辅助

放到：

- `map_core.js`

---

## 5. 当前拆分后的收益

和之前单个 `map_core.js` 相比，现在已经具备这些好处：

- 地图初始化不再和动作逻辑混写
- 目标状态和面板渲染已经分开
- 测向 / 精准打击 / 宽频干扰不再和地图壳耦在一起
- Qt 桥接入口已经集中，不再分散在多个文件
- 后续继续改首页功能时，更容易知道应该改哪一层

---

## 6. 后续继续演进的建议

当前这轮拆分已经够用，可以先稳定使用。

如果后面还要继续细化，可以优先考虑：

- 把 `map_core.js` 里剩余的格式化函数再抽成 `map_formatters.js`
- 如果目标点位推算逻辑变复杂，再把 marker / position 处理抽成 `target_map_renderer.js`
- 如果后面动作更多，可以把 `target_actions.js` 再细拆成：
  - `direction_action.js`
  - `precision_strike_action.js`
  - `wide_jam_action.js`

但现阶段不建议为了拆而拆，当前 6 文件结构已经足够支撑后续维护。

---

## 7. 一句话原则

首页前端后续改动时，优先遵守这条规则：

- 地图壳放 `map_bootstrap.js`
- 状态放 `target_store.js`
- 渲染放 `target_panel.js`
- 动作放 `target_actions.js`
- Qt 入口放 `qt_bridge.js`
- 剩余地图业务辅助放 `map_core.js`
