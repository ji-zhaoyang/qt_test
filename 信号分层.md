# 信号分层

## 目标

这份文档用于把当前工程里的信号按职责分成两类：

1. 系统级信号
2. 具体业务信号

目的不是做语法整理，而是为了后面继续重构和接数据库时，能快速判断：

- 哪些信号应该继续留在 `AppController`
- 哪些信号应该放到 `Coordinator`
- 哪些信号将来应该继续下沉到 `Service`

---

## 一、系统级信号

### 定义

系统级信号描述的是“整个应用壳层”或“设备连接层”的状态变化。

它们通常有这些特征：

- 不直接对应某个单独协议功能
- 不只服务某一个页面
- 更适合给顶部栏、主窗口、全局状态栏之类的模块使用
- 一般不直接对应数据库第一批落库对象

### 当前项目中的系统级信号

#### 1. `AppController::deviceStatusChanged()`

位置：

- `src/app_controller.h`
- `src/app_controller.cpp`

语义：

- 设备连接状态发生变化

当前关联内容：

- 正在连接
- 已连接
- 已断开
- 错误
- 最后一条错误信息

说明：

- 这是“状态已经变了”的通知
- 不直接携带详细状态内容
- 消费方应该自己去读取 `AppController::deviceStatus()`

推荐分层：

- 当前继续留在 `AppController`

#### 2. `AppController::deviceStatusInfoUpdated(const QJsonObject &deviceInfo)`

位置：

- `src/app_controller.h`
- `src/app_controller.cpp`

语义：

- 收到了一份设备基础信息，可用于全局展示

当前关联内容：

- 当前主要用于顶部栏设备信息展示

说明：

- 这是“设备信息数据推送”
- 和 `deviceStatusChanged()` 不一样，它直接带了完整数据

推荐分层：

- 当前继续留在 `AppController`

#### 3. `TcpManager::connected()`

语义：

- TCP 连接建立成功

推荐分层：

- 属于系统连接层
- 不建议直接下沉到某个业务 service

#### 4. `TcpManager::disconnected()`

语义：

- TCP 连接断开

推荐分层：

- 属于系统连接层

#### 5. `TcpManager::errorOccurred(const QString &errorStr)`

语义：

- TCP 或通信层出现错误

推荐分层：

- 属于系统连接层

#### 6. `HomePage::fullscreenChanged(bool isFullscreen)`

语义：

- 首页展示壳层状态改变

说明：

- 这是 UI 壳层信号，不是业务功能信号

推荐分层：

- 当前继续留在页面层/协调层边界

---

## 二、具体业务信号

### 定义

具体业务信号描述的是某个明确功能的：

- 发起请求
- 返回结果
- 主动上报
- 状态回填

它们通常有这些特征：

- 一眼能看出对应哪个功能
- 强绑定某类协议
- 通常更适合作为数据库第一批落库候选

---

## 三、首页相关业务信号

### 1. 首页动作请求信号

位置：

- `src/views/home/home_page.h`

包括：

- `commJammingToggled(bool checked)`
- `navJammingToggled(bool checked)`
- `requestDroneDirectionFinding(bool enabled, quint32 targetId)`
- `requestDronePrecisionStrike(bool enabled, quint32 timestamp, const QString &sn, int type, quint32 targetId)`
- `requestDroneWideBandJamming(bool enabled, quint32 frequencyKhz, const QString &sn, quint32 targetId)`

语义：

- 首页发起某个明确动作

对应业务：

- 通信干扰
- 导航干扰
- 测向
- 精准打击
- 宽频干扰

推荐分层：

- 当前放在 `HomeCoordinator`
- 后续如继续细拆，可下沉到 `DroneOpsService` / `DeviceOpsService`

### 2. 首页结果/上报信号

位置：

- `src/network/core/tcp_manager.h`

包括：

- `droneTargetReported(const QJsonObject &targetInfo)`
- `droneDirectionFindingResponse(...)`
- `droneDirectionPowerReported(const QJsonObject &reportData)`
- `dronePrecisionStrikeResponse(...)`
- `droneWideBandJammingResponse(...)`
- `deviceJammingModeSetResponse(...)`
- `deviceJammingModeReported(...)`

语义：

- 设备针对首页相关功能返回结果，或主动上报数据

推荐分层：

- 当前通过 `HomeCoordinator` 转给 `HomePage`
- 后续如果继续拆 `TcpManager`，建议放进：
  - `DroneOpsService`
  - `DeviceOpsService`

数据库优先级：

- 高

适合优先落库的候选：

- 精准打击操作结果
- 宽频干扰操作结果
- 无人机目标历史
- 设备干扰状态变化

---

## 四、设置页相关业务信号

### 1. 设置页请求信号

位置：

- `src/views/settings/settings_page.h`

主要模式：

- `requestSave...`
- `requestQuery...`
- 少量 `requestOpen...` / `requestClose...` / `requestStart...`

代表例子：

- `requestSaveGps(...)`
- `requestQueryGps()`
- `requestSaveDetectBands(...)`
- `requestQueryDetectBands()`
- `requestSaveFeatureModes(...)`
- `requestQueryFeatureModes()`
- `requestSaveSystemTime(...)`
- `requestRebootDevice()`
- `requestUploadPatternFile(...)`

语义：

- 设置页发起具体配置保存、查询或控制操作

推荐分层：

- 当前放在 `SettingsCoordinator`
- 后续可按领域拆分到：
  - `DeviceSettingsService`
  - `FirmwareService`
  - `SpectrumService`
  - `ModelLibraryService`

### 2. 设置页结果回填信号

位置：

- `src/network/core/tcp_manager.h`

主要模式：

- `...Queried`
- `...SetResponse`
- `...Reported`

代表例子：

- `deviceGpsQueried(...)`
- `deviceGpsSetResponse(...)`
- `detectBandsQueried(...)`
- `featureModesSetResponse(...)`
- `fullSpectrumReported(...)`
- `modelLibraryRecordsQueried(...)`
- `powerAmplifierParamsQueried(...)`
- `directionCalibrationValuesQueried(...)`

语义：

- 设置页具体业务结果回填

推荐分层：

- 当前放在 `SettingsCoordinator`
- 后续可继续下沉到按业务拆分的 service

数据库优先级：

- 中

说明：

- 大多数设置查询结果不一定需要第一时间落库
- 但“设置变更记录”很适合后面做数据库时补进去

---

## 五、当前最适合保留在 `AppController` 的信号

当前建议继续保留在 `AppController` 的，主要只有两类：

### 1. 连接状态广播

- `deviceStatusChanged()`

原因：

- 它描述的是系统连接状态
- 不是某个页面的具体业务

### 2. 设备基础信息广播

- `deviceStatusInfoUpdated(const QJsonObject &deviceInfo)`

原因：

- 它服务的是全局展示层
- 当前主要给 `TopNavBar`
- 后续如果还有别的全局模块需要，也适合继续复用

---

## 六、当前最适合下沉到 `Coordinator` 的信号

### 下沉到 `HomeCoordinator`

- 首页动作请求
- 首页干扰控制
- 首页测向
- 首页精准打击
- 首页宽频干扰
- 首页目标上报

### 下沉到 `SettingsCoordinator`

- 设置页保存请求
- 设置页查询请求
- 设置页结果回填
- 设置页设备连接恢复/丢失
- 设置页系统时间保存

---

## 七、将来最适合继续下沉到 `Service` 的信号

当后面开始拆 `TcpManager` 时，建议按业务组继续下沉：

### 可下沉到 `DroneOpsService`

- `droneTargetReported`
- `droneDirectionFindingResponse`
- `droneDirectionPowerReported`
- `dronePrecisionStrikeResponse`
- `droneWideBandJammingResponse`

### 可下沉到 `DeviceOpsService`

- `deviceJammingModeSetResponse`
- `deviceJammingModeReported`
- `deviceRebootResponse`

### 可下沉到 `SettingsDomainService`

- 各类 `...Queried`
- 各类 `...SetResponse`

---

## 八、数据库接入时怎么用这份分类

### 第一批适合考虑落库的

优先看“具体业务信号”，不要优先看系统级信号。

推荐优先考虑：

- 无人机目标历史
- 精准打击操作日志
- 宽频干扰操作日志
- 通信/导航干扰状态变化
- 设置项变更记录

### 第一批不建议优先落库的

- `deviceStatusChanged()`
- `connected()`
- `disconnected()`
- `errorOccurred(...)`

原因：

- 这些更适合先服务 UI 壳层和运行期状态展示
- 如果后面确实要记录在线/离线历史，再单独加“连接状态日志”即可

---

## 九、快速判断规则

如果一个信号满足下面任意一点，通常更像“系统级信号”：

- 描述的是连接状态
- 描述的是全局设备信息
- 不绑定某个页面上的具体功能按钮

如果一个信号满足下面任意一点，通常更像“具体业务信号”：

- 名字里直接带业务词
- 对应明确协议号
- 只服务某一个功能模块
- 可以直接转成一条操作记录或业务记录

---

## 十、当前结论

当前工程里：

- `AppController` 只建议继续持有少量系统级广播
- 首页相关信号应继续留在 `HomeCoordinator`
- 设置页相关信号应继续留在 `SettingsCoordinator`
- 后续数据库设计，应优先围绕“具体业务信号”展开，而不是从系统级信号入手
