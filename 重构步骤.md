# 重构步骤

## 目标

在不影响现有功能的前提下，先把工程从“能跑”整理成“可持续扩展”，再接数据库功能。

这次重构的核心目标有 4 个：

1. 降低首页 Qt 和网页 JS 之间的耦合
2. 降低 `AppController` 和 `TcpManager` 的职责膨胀
3. 把持久化逻辑从界面层移出去
4. 为后续数据库接入预留明确的分层位置

## 当前结构判断

当前工程不是完全混乱，已经有基本分层：

- `MainWindow` 负责页面装配
- `AppController` 负责页面和网络层连接
- `TcpManager` 负责 TCP 与协议
- `HomePage` 负责 Qt 首页
- `assets/web/js/map_core.js` 负责地图和右侧无人机面板

但现在已经进入“继续加功能会越来越难维护”的阶段，主要问题是：

- 首页和网页靠 `document.title` + `runJavaScript()` 传命令，字符串桥接太脆弱
- `AppController::setupConnections()` 已经变成总线汇聚点，后续加数据库会继续膨胀
- `TcpManager` 同时承担传输、协议打包、业务 pending 状态、协议分发
- `SettingsPage` 已经直接做了持久化，说明“数据落盘”开始往 UI 层渗透
- `map_core.js` 已经承担地图、目标缓存、按钮状态、协议命令拼接，单文件继续长大

## 建议顺序

建议严格按下面顺序做，不要跳步。

原因：

- 先拆桥接，再拆控制器，能避免数据库功能继续加在旧耦合点上
- 先明确“数据该放哪一层”，数据库接入时就不会直接塞进页面类

---

## 第 1 步：先画出当前责任边界

### 要做什么

给几个核心类写一句“唯一职责”，先统一认知。

### 建议结果

- `MainWindow`：只负责组装页面和顶层导航
- `HomePage`：只负责首页 Qt 容器和展示
- `SettingsPage`：只负责设置页聚合和展示
- `AppController`：只负责协调，不负责业务细节
- `TcpManager`：只负责通信和协议收发
- `map_core.js`：只负责网页地图展示和页面内交互

### 这一步不要做的事

- 不改业务逻辑
- 不改协议内容
- 不接数据库

---

## 第 2 步：先拆首页 Qt <-> Web 的字符串桥接

### 当前问题

现在首页依赖：

- `HomePage::setupConnections()` 解析 `CMD:DIRECTION_FINDING`
- `HomePage::setupConnections()` 解析 `CMD:PRECISION_STRIKE`
- `HomePage::setupConnections()` 解析 `CMD:WIDE_JAM`
- 多个 `dispatch...ToMap()` 再反向 `runJavaScript()`

这会导致每新增一个功能，都要同时改：

- `map_core.js`
- `home_page.cpp`
- `home_page.h`
- `app_controller.cpp`
- `tcp_manager.*`

### 重构目标

把“字符串命令”统一收口，不要让 `HomePage` 直接识别每个业务命令。

### 推荐做法

新增一个首页桥接层，例如：

- `src/views/home/home_web_bridge.h`
- `src/views/home/home_web_bridge.cpp`

这个类只做两件事：

1. 解析网页发来的命令
2. 把 Qt 结果封装后发回网页

### 第一阶段先做到

- `HomePage` 不再直接 `split(':')`
- `HomePage` 不再直接持有每个具体业务的 `dispatch...ToMap()`
- `HomeWebBridge` 提供统一接口，例如：
  - `parseWebCommand(title)`
  - `sendEventToWeb(name, payload)`

### 第二阶段再考虑

如果 Qt 版本和环境允许，再把这层进一步替换成更规范的桥接方式，比如 `QWebChannel`

---

## 第 3 步：拆 `AppController`，不要继续做总线中心

### 当前问题

`AppController::setupConnections()` 里已经堆了大量跨模块 connect。

这会直接导致：

- 新功能要改 `AppController`
- 新页面要改 `AppController`
- 新数据源要改 `AppController`
- 将来数据库接入也大概率继续往这里堆

### 重构目标

把“按业务分组的协调逻辑”从 `AppController` 拆出去。

### 推荐拆法

先按页面或业务分 3 个协调器：

- `HomeCoordinator`
  - 管首页、地图、目标、测向、精准打击、宽频干扰
- `SettingsCoordinator`
  - 管设置页保存/查询/回填
- `DeviceStatusCoordinator`
  - 管连接状态、设备状态、顶部栏状态

### AppController 最终应保留

- 初始化这些 coordinator
- 提供极少量全局入口

### AppController 最终不应继续保留

- 大量逐条 `connect(...)`
- 每个业务协议都知道一点
- 每个页面细节都掺和一点

---

## 第 4 步：拆 `TcpManager`，把协议能力和传输层分开

### 当前问题

`TcpManager` 现在同时做了：

- socket 生命周期
- 发包
- 收包
- 协议分发
- 具体业务命令构造
- 具体业务应答解析
- 业务 pending 状态保存

这会导致它越来越像一个“总协议中心”，数据库接入后如果继续照这个方式做，很容易演变成：

- 网络收了数据
- 在 `TcpManager` 里直接顺手存库

这会非常难维护。

### 重构目标

把 `TcpManager` 收缩成“通信底座”，业务协议拆出去。

### 推荐拆法

#### 4.1 保留 `TcpManager`

只保留：

- 连接
- 重连
- 缓冲区
- 基础发包
- 帧分包
- 协议分发入口

#### 4.2 新增协议服务层

例如：

- `DroneOpsService`
  - 测向
  - 精准打击
  - 宽频干扰
- `DeviceOpsService`
  - 通信干扰
  - 导航干扰
  - 重启
- `SettingsProtocolService`
  - 各设置项保存/查询

### 推荐原则

- `TcpManager` 不持有越来越多 `pendingXxx`
- 每个服务只管自己协议的数据结构和状态
- 每个服务只暴露“领域意义”的接口，不暴露底层帧细节给页面

---

## 第 5 步：把持久化从页面层拿走

### 当前问题

`SettingsPage` 已经在自己做：

- 密码 hash
- `QSettings`
- `auth.ini` 文件路径选择

这说明“数据保存”已经落在 UI 层了。

如果数据库也按这个模式加，很容易变成：

- `SettingsPage` 直接连数据库
- `HomePage` 直接写数据库
- 某个 JS 结果回 Qt 后直接存库

### 重构目标

所有持久化逻辑统一进入 repository/service 层，页面层只发请求，不自己存数据。

### 推荐拆法

先把当前密码配置抽出去：

- `src/repositories/auth_repository.h`
- `src/repositories/auth_repository.cpp`

让这个类负责：

- `loadPasswordHash(role)`
- `savePasswordHash(role, passwordHash)`

然后再加：

- `AuthService`
  - 认证
  - 校验
  - 修改密码

这样未来把 `QSettings` 替换成数据库时，只改 repository，不改页面。

---

## 第 6 步：给数据库预留标准入口

### 不要直接做

- 不要在 `HomePage` 里写数据库
- 不要在 `SettingsPage` 里写数据库
- 不要在 `TcpManager` 里写数据库

### 应该新增

- `src/repositories/`
- `src/services/`
- `src/models/`

### 推荐第一批抽象

#### models

- `DroneTargetRecord`
- `DeviceEventRecord`
- `ActionLogRecord`

#### repositories

- `DroneTargetRepository`
- `DeviceEventRepository`
- `ActionLogRepository`

#### services

- `DroneTargetService`
  - 目标缓存
  - 目标转换
  - 目标存储
- `ActionService`
  - 精准打击记录
  - 宽频干扰记录
  - 操作状态流转

### 数据流应该变成

1. 协议层解析出结构化数据
2. service 做业务判断
3. repository 决定是否落库
4. 页面只订阅展示结果

---

## 第 7 步：整理网页前端 `map_core.js`

### 当前问题

`map_core.js` 现在同时做了：

- 地图初始化
- 目标缓存
- 列表渲染
- 详情渲染
- 测向逻辑
- 精准打击逻辑
- 宽频干扰逻辑
- 提示逻辑
- 右侧栏状态逻辑

### 重构目标

按功能拆文件，而不是继续堆在 `map_core.js`。

### 推荐拆法

- `map_bootstrap.js`
  - 地图初始化
  - 图层切换
- `target_store.js`
  - 目标缓存
  - 目标选中
  - 目标状态
- `target_panel.js`
  - 右侧列表
  - 详情渲染
- `target_actions.js`
  - 测向
  - 精准打击
  - 宽频干扰
- `qt_bridge.js`
  - 网页向 Qt 发命令
  - Qt 回调入口

### 注意

这里不要求你一次性拆完，可以先把“状态”和“渲染”拆开。

---

## 第 8 步：统一状态模型，再开始接数据库

### 这一步的目标

在真正接数据库之前，先把“内存状态”统一好。

### 当前状态分散位置

- `HomePage` 持有 `pendingDroneTargets`
- `map_core.js` 持有 `droneTargets`
- `precisionStrikeState`
- `wideJamState`
- `directionFindingState`
- `TcpManager` 持有很多 pending 状态

### 风险

如果数据库接入前不统一状态来源，后面会出现：

- 页面状态一份
- 网络状态一份
- 数据库存储状态一份

三份状态很容易打架。

### 推荐做法

先定义“谁是真正状态源”：

- 协议实时状态：service 层
- UI 显示状态：页面层缓存
- 历史持久化：repository/database

---

## 第 9 步：数据库接入顺序

等前面步骤做完，再开始数据库。

### 推荐顺序

#### 9.1 先接最简单的

- 动作日志表
  - 记录精准打击
  - 记录宽频干扰
  - 记录通信/导航干扰开关

#### 9.2 再接目标历史

- 无人机目标历史表
  - 上报时间
  - 序号/序列号
  - 类型
  - 频率
  - 距离
  - 经纬度

#### 9.3 最后接配置持久化

- 用户密码
- 页面参数
- 设置参数快照

### 原因

动作日志和目标历史最容易独立落地，不会太早把数据库耦合进控制链路。

---

## 第 10 步：每一步完成后的验收标准

每完成一步，都用下面 4 条验收：

1. 新增一个功能时，是否比现在少改文件
2. 页面层是否不再直接处理持久化
3. 协议层是否不再继续膨胀
4. 同类功能是否能按固定模板接入

如果这 4 条没有明显改善，就不要急着开始下一步。

---

## 最推荐的实际执行顺序

如果你准备一点点做，建议按这个顺序：

1. 新建 `重构分层说明`，明确每层职责
2. 抽 `HomeWebBridge`
3. 抽 `AuthRepository` + `AuthService`
4. 拆 `AppController` 为 `HomeCoordinator` / `SettingsCoordinator`
5. 拆 `TcpManager` 的业务协议能力
6. 拆 `map_core.js`
7. 统一状态来源
8. 接数据库

---

## 你现在最应该先做哪一步

如果只选一步，我建议先做：

**第 2 步：先拆首页 Qt <-> Web 的字符串桥接**

原因：

- 这是当前首页功能继续扩展时最明显的耦合点
- 精准打击、宽频干扰、测向都已经把这个问题暴露出来了
- 后面数据库如果要展示动作历史或目标状态，这层不先整理，新增功能还会继续串着改

---

## 备注

这份步骤文档是“先不改业务逻辑”的重构计划。

建议你按“一步一提交”的方式推进，每做完一步就：

- 编译
- 自测首页
- 自测设置页
- 确认协议功能没有回归

不要一次性大改。
